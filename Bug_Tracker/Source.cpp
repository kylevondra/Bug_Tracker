#include "Bug.h"
#include <iostream>
#include <string>

using namespace std;

int main() {
	vector<Bug> Bugs;
	for (int i = 0; i < 100; i++) {
		Bug a = a.rBug();
		a.printBug();
		Bugs.push_back(a);
	};

	Bug b = b.rBug();
	b.fileBug(Bugs);

	return 0;
}
